{
  "name": "side-quests-fullstack",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd sideQuests && npm run dev\" \"cd server && npm run dev\"",
    "dev:frontend": "cd sideQuests && npm run dev",
    "dev:backend": "cd server && npm run dev",
    "build": "cd sideQuests && npm run build",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "nodemon": "^3.1.11"
  }
}
